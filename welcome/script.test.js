// import { describe } from 'jest';
// import { fetchEvents } from './script';

// global.fetch = jest.fn(() =>
//     Promise.resolve({
//         json: () => Promise.resolve({ mockedData: 'mocked response' }),
//     })
// );

// describe('fetchEventsTicketmaster', () => {
//     it("fetches data from the API endpoint", async () => {
//         const data = await fetchEvents();

//         expect(data).toEqual({ mockedData: 'mocked response' });
//     });
// });